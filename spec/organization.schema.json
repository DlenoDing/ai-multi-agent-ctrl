{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dleno.local/spec/organization.schema.json",
  "title": "Organization",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "orgId",
    "name",
    "status",
    "quotas",
    "createdAt",
    "updatedAt"
  ],
  "properties": {
    "schemaVersion": {
      "const": "organization/v1"
    },
    "orgId": {
      "type": "string",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "enum": [
        "active",
        "suspended"
      ]
    },
    "quotas": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "maxMembers",
        "maxProjects",
        "maxTaskGroups",
        "maxAgents"
      ],
      "properties": {
        "maxMembers": {
          "type": "integer",
          "minimum": 1
        },
        "maxProjects": {
          "type": "integer",
          "minimum": 1
        },
        "maxTaskGroups": {
          "type": "integer",
          "minimum": 1
        },
        "maxAgents": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "usage": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "members": {
          "type": "integer",
          "minimum": 0
        },
        "projects": {
          "type": "integer",
          "minimum": 0
        },
        "taskGroups": {
          "type": "integer",
          "minimum": 0
        },
        "agents": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "initialAdminAccountId": {
      "type": "string"
    },
    "createdBy": {
      "type": "string"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    }
  }
}
