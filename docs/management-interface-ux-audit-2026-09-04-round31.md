# 第三十一轮管理界面与操作闭环复验

## 复验范围

- 系统管理、组织管理、项目管理三类空间是否继续保持职责分离。
- 项目概览、项目设置、项目 AI 智能体、执行监控这些高频入口是否能让中文用户快速判断下一步。
- Agent 查看、注册脚本来源、一次性加入令牌、远程 MCP、Skill 同步、节点自检与运行监控是否形成可操作闭环。
- 本轮改动是否保持简单、稳定、高性能：不新增接口、不新增后台轮询、不改变服务端数据模型，只优化入口信息架构和防回归检查。

## 本轮发现

前几轮已经完成了关键架构方向：常规 Agent 注册只在项目级「AI 智能体」页发生；系统级和组织级页面只做全局治理、审计和节点控制；执行监控页能看实时事件、派发、会话、节点和关闭门。

继续从普通中文项目负责人视角推演后，仍有几处弱点：

- 项目概览的「AI 智能体」卡片有入口，但没有明确写出完整路径和服务端脚本来源；第一次使用的人仍可能不知道脚本从哪里来。
- 项目操作路径里的「执行准备」只说“先注册 agent”，没有把“签发一次性加入令牌、复制服务端安装脚本、确认远程 MCP/Skill 生效”串成一条闭环。
- 执行监控的「节点」处置卡片在无节点时只写“先到 AI 智能体页注册 agent”，没有完整路径；在有异常节点时也没有提醒回项目 Agent 页点“刷新自检”。
- 项目设置里的「Agent 接入」卡片仍像一个附属入口，未强调配置页不处理注册表单、实际注册要去项目级 Agent 页。
- 只读互审进一步指出：组织页和系统页跳向项目 Agent 注册时必须提醒先确认目标项目；若数据通道未来出现跨项目待办，按钮不能只进当前项目。
- Claude CLI 复审进一步纠偏：服务端对节点「吊销 / 立即切断」按项目作用域 `agent:activate` 判权，不能把所有有权账号都引到组织页；当前 tasks 视图按项目过滤，待办面板不能恒写“跨全部项目统计”或“先进入当前项目”。

这些问题不影响服务端核心链路，但会影响中文用户在界面上快速找到真实操作点，属于 P2 级界面信息架构问题。

## 优化方案

1. 保留现有菜单结构，不新增页面、不拆接口，避免把系统复杂度继续抬高。
2. 在高频入口处统一使用完整路径：「项目管理」→「AI 智能体」→「注册 agent」。
3. 将 Agent 首次接入描述统一为：签发一次性加入令牌、复制服务端安装脚本、在 agent 主机执行、回项目节点列表确认在线/准入/远程 MCP/Skill 生效。
4. 将异常节点处置描述统一为：先看执行监控定位异常，再回项目「AI 智能体」节点行点「刷新自检」或让组织管理员做跨项目治理。
5. 补充 console 行为门，确保项目概览、项目设置、执行监控三个入口都不能退回含糊文案。

## 实施记录

- 已将项目概览「AI 智能体」卡片和「执行准备」路径改为完整闭环：进入「项目管理」→「AI 智能体」→「注册 agent」签发加入令牌、复制服务端安装脚本，再确认远程 MCP 和 Skill 工作集生效。
- 已将执行监控「节点」处置卡片拆清两种状态：
  - 无节点：指向完整项目注册路径和服务端安装脚本来源。
  - 异常节点：先恢复 agent 主机/进程心跳，能力修好后到项目「AI 智能体」节点行点「刷新自检」。
- 已将项目设置中的 Agent 接入口改成明确的跨页入口：注册脚本只在项目 AI 智能体页签发成功弹窗显示，配置页不再让用户误以为这里能处理注册表单。
- 已按账号可达性处理项目级节点危险操作：系统管理员和无组织治理页入口但有 `agent:activate` 的项目账号仍能在项目页看到服务端允许的「吊销 / 立即切断」；组织管理员在项目页看到组织治理出口，避免重复暴露同一组危险按钮。
- 已将组织级 AI 智能体看板标题从“接入操作看板”改为“治理操作看板”，并在组织/系统跨空间入口上提醒进入项目后先确认目标项目。
- 已修正「待你处理」口径：当前服务端 tasks 视图按当前项目过滤，页面改为“按当前项目视图统计”；只有未来数据真的包含跨项目待办时，按钮才先切到待办所在项目再进入审核、监控或指令页。
- 已减少项目 AI 智能体卡片视图无节点时的重复空态提示。
- 已补充 console 行为门，覆盖项目概览脚本来源、项目设置职责分区、项目级危险治理可达性、组织治理看板命名、执行监控无节点/异常节点处置、当前项目待办口径与跨项目待办切项目兜底。

## 验证计划

- `node --check apps/control-plane-ui/public/app.js`：通过。
- `node --check scripts/console-behaviour-check.mjs`：通过。
- `npm run console-behaviour-gate`：通过，602 条断言全部通过。
- `npm run validate`：通过。
- `npm run docker:doctor`：通过；首次运行时本机已有 `npm start` 占用 4317 端口，停止本地服务后 Docker/中央 MCP/安装脚本/PostgreSQL 状态库检查全部通过。
- `npm run mutation-gate`：通过，1362 条守卫均已证明其测试具备判别力。
- 本地浏览器桌面与移动视口走查项目概览、项目设置、项目 AI 智能体、执行监控：通过；桌面 1440px 与移动 390px 下关键页面无横向溢出，项目级 Agent 页在系统管理员视角保留服务端允许的吊销类操作，组织管理员视角展示组织治理出口。
- 子代理只读互审与 Claude CLI 只读复审本轮终态：通过；互审指出的完整路径、治理边界、待办口径、异常节点指路和无节点重复空态均已处理，Claude 对项目级危险操作可达性与 tasks 项目过滤口径的纠偏已落地。
