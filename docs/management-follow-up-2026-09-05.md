# 管理交互与执行链路复验

本次按用户列出的未闭环项实施，保留既定名称与负责人／管理员职责定义。

## 改进及验收

| 项目 | 改进方案 | 验收依据 | 状态 |
| --- | --- | --- | --- |
| 成员页引导过重 | 成员列表置顶，统计合并为紧凑栏，新增／授权可直接到达，帮助收在操作区之后 | 两种成员页真实渲染、桌面与窄屏首屏检查 | 进行中 |
| 仓库仅验证读权限 | 保留读取检测，增加明确的读写验证；真实创建唯一临时分支并删除，单独报告写入与清理结果；不将预检当成受保护分支授权证明 | 裸仓库真实写入、拒绝写入及清理失败测试；管理端实际请求 | 进行中 |
| 凭证拒绝无可点击入口 | 监控的派发、会话与事件错误提供本项目仓库配置入口；根据可见项目定位，不接受事件内任意 URL | 真错误渲染及点击定位、越界项目回归 | 进行中 |
| 组织 Agent 实际调配 | 验证组织档案候选、模型决策、节点派发及认领链路，组织外候选必须排除 | 隔离运行态的端到端测试 | 进行中 |
| 组织管理员操作 | 创建成员、授予项目角色、授予任务组角色并核对成员登录结果 | 浏览器实际提交与服务器保存结果 | 待执行 |
| 多组织可见性 | 第二组织管理员登录，核对成员、项目、Agent 页面和跨组织请求 | 浏览器走查与权限断言 | 待执行 |
| 窄屏 | 检查导航、列表、授权表单、仓库配置与错误处置 | 桌面、平板与手机截图及溢出检查 | 待执行 |

## 实施约束

- 使用隔离验证数据，避免修改已有项目和真实仓库。
- 仓库写入检测只上传无业务内容的测试提交；每次创建独立测试引用，清理失败必须显示待清理引用。
- 组织 Agent 档案与运行节点分别核验，不能由页面可见推断可以派发。
- 结果以真实行为为准，既有断言若锁定旧界面堆叠顺序则更新为列表可达性与权限正确性。
- 稳定步骤通过验证后提交推送；最终记录实测范围和未能证明的边界。
