# 菜单优先中文管理控制台终态改造

日期：2026-09-06  
状态：已完成并通过终态复验

## 结论

上一轮已经补齐系统／组织／项目边界、对象详情和真实执行链，但控制台的主要认知模型仍然是“父页面 + 页面内栏目”：项目空间只有九个父菜单，点击后才临时出现三至七个二级栏目；移动端则在内容区出现“当前栏目”选择器。用户必须先记住“注册 Agent 属于项目 Agent 的注册栏目”“质量证据属于执行监控的产出栏目”“任务组权限属于成员权限的任务组栏目”，才能找到操作。

这仍然是 Tab 思维，只是把 Tab 从内容顶部挪到了侧栏。终态应改为菜单优先：每个可独立理解、独立授权、独立创建或独立处置的功能都拥有稳定菜单项、明确页面标题和可复制地址。页面内只保留对象详情自身的局部上下文，不再把主要功能隐藏在父页面栏目里。

## 设计原则

1. **按管理对象和动作命名**：使用“成员账户、权限矩阵、Agent 档案、运行节点、注册 Agent、任务组、新建任务组、任务、新建任务、项目监控、执行会话、实时事件、产出验收、待我审核、授权复核、下达指令、指令记录、仓库凭据、系统规则”等直接名称。
2. **列表、创建、详情分开**：列表页用于查找与进入对象；创建入口是稳定菜单或列表页主按钮；详情页只处理当前对象，不把其它列表继续堆在下面。
3. **系统、组织、项目三套菜单彻底分离**：系统管理不出现组织子账户、项目授权、Agent 或执行入口；组织管理不替项目执行；项目管理不出现跨组织治理。
4. **桌面菜单稳定可见，窄屏单一选择器**：桌面侧栏始终列出当前管理空间的全部功能并按业务阶段分组；移动端只显示一个当前位置选择器，不渲染横向滚动菜单和第二层栏目选择器。
5. **原有权限不变**：菜单可见性只能缩小到当前账号可执行或可读取的范围，不能因为重排菜单扩大服务端权限。所有写操作继续由现有 API、状态机、幂等和审计守卫处理。
6. **AI-native 执行不变**：人只创建项目／任务组／任务、管理账号与 Agent、启动／暂停、审核／复核和下达独立指令；任务分析、角色／模型／Skill 选择、派发、执行、回送和结果收口继续由 AI 总控与 Agent Runtime 自动完成。

## 终态菜单

### 系统管理

| 分组 | 稳定入口 | 职责 |
| --- | --- | --- |
| 运行 | 系统概览、审计日志、维护操作 | 平台运行与系统审计 |
| 组织治理 | 组织列表、开通组织 | 组织启停、配额、子账户数量、初始组织管理员 |
| 平台能力 | 运行参数、模型能力、技能源、调度协议 | 服务器级配置和只读能力目录 |
| 使用说明 | 组织治理说明、平台能力说明 | 保留原说明内容并提供稳定地址 |

系统空间不提供组织成员创建、项目／任务组授权、项目创建、Agent 档案或运行节点管理。

### 组织管理

| 分组 | 稳定入口 | 职责 |
| --- | --- | --- |
| 组织总览 | 组织概览 | 配额、项目、成员和共享资源概况 |
| 成员与权限 | 成员账户、创建成员、权限矩阵 | 子账户生命周期以及项目／任务组两级权限 |
| 项目目录 | 项目列表、创建项目、项目授权 | 组织内项目创建、归档和成员落位 |
| 共享 Agent | 共享 Agent 档案、共享运行节点、注册共享节点、加入令牌 | 组织级逻辑角色和跨项目节点 |
| 使用说明 | 组织操作说明、成员授权说明、项目治理说明、共享 Agent 说明 | 对应治理流程与边界说明 |

### 项目管理

| 分组 | 稳定入口 | 职责 |
| --- | --- | --- |
| 项目总览 | 项目概览、最新执行、仓库产出 | 项目进度、健康度和当前下一步 |
| 成员与权限 | 项目成员、任务组权限 | 项目角色和任务组控制／审核／观察权限 |
| Agent | Agent 档案、运行节点、注册 Agent | 项目专属档案、组织共享档案、项目／共享节点 |
| 工作推进 | 任务组、新建任务组、任务、新建任务 | 对象列表、进度和创建入口 |
| 执行观测 | 项目监控、执行会话、实时事件、节点控制、产出验收、阻塞与门禁 | 项目和任务组两级实时监控及处置 |
| 人工控制 | 待我审核、授权复核、审核历史、下达指令、指令记录 | 必要的人定、复核和独立结构化指令 |
| 项目治理 | 仓库凭据、基线资料、角色与 Skill、系统规则、业务规则 | 项目执行输入和 Git 产出配置 |
| 使用说明 | 项目操作说明、项目授权说明、Agent 运行说明、任务组说明、监控链路说明、审核流程说明、指令通道说明、项目配置说明 | 原说明面板的稳定入口 |

## 页面与路由

保留现有服务端页面族和 `?pane=` 兼容编码，但 `pane` 不再作为临时 Tab，而是稳定功能入口的地址组成部分。每个菜单项直接写入对应地址，页面标题使用当前功能名，不再显示父页面名后再补一个重复栏目标题。

对象详情继续使用路径段：

```text
#/system/organizations/:orgId
#/organization/members/:accountId
#/organization/agents/runtime/:nodeId
#/project/:projectId/task-groups/:taskGroupId
#/project/:projectId/tasks/:taskGroupId/:workItemId
#/project/:projectId/agents/:agentId
#/project/:projectId/agents/runtime/:nodeId
#/project/:projectId/monitor/:taskGroupId/session/:sessionId
#/project/:projectId/monitor/:taskGroupId/dispatch/:dispatchId
```

## 页面结构

- 页头：当前具体功能名称、简短职责说明、账号和刷新。
- 层级路径：组织／项目／任务组／任务／执行对象。
- 内容首段：当前对象、状态、关键指标和唯一主要动作。
- 列表：紧凑、可筛选、按更新时间或创建时间倒序；名称直接进入详情。
- 创建：字段按中文业务顺序排列，提交后直接进入新对象详情。
- 详情：纵向信息带，固定返回上级；复杂协议字段按需展开。
- 空态：解释为什么为空并提供唯一下一步，不重复整套流程说明。

## 实施顺序

1. [x] 重构导航目录：把工作区栏目提升为稳定菜单项，提供当前功能元数据和权限过滤。
2. [x] 删除桌面嵌套栏目导航和移动端第二层栏目选择器；新增单一移动功能选择器。
3. [x] 修正菜单点击、浏览器历史、项目切换、对象详情返回和未保存表单保护。
4. [x] 调整列表／创建／详情入口，使项目、任务组、任务、Agent、审核和指令均可从明确菜单直接到达。
5. [x] 完成系统管理员、组织管理员、项目负责人、审核者和只读观察者的桌面／390px 真实操作。
6. [x] 完成行为、契约、权限、变异、全量、Docker 和外部复审，并逐步提交推送。

## 复验与互审

### 第一轮外部审查

独立只读审查发现五项问题，均已复现和修复：

1. 当前叶子菜单因同目标早退而保留旧任务组范围。
2. 任务组任务列表的分页游标泄漏到项目级任务列表。
3. 待办红点与处置按钮只按父页面聚合，不能区分待我审核、授权复核、产出验收、阻塞与门禁和指令记录。
4. 十四个说明 workspace 在删除旧 Tab 后失去生产入口。
5. 功能看板跨叶子跳转使用 `replaceState`，浏览器后退跳过来源功能。

对应修复提交：`4bd91d4`、`3588190`、`56db2b3`。测试强化提交：`162aade`。

### 第二轮外部审查

复审确认第一轮问题主路径已闭合，未发现 P0/P1；另发现项目首页“当前下一步”仍按父页面进入审核或监控。终态实现按待办类型选择精确叶子：定稿进入待我审核，授权／审批／发现进入授权复核，混合待办进入待办汇总，质量门进入产出验收，其它执行阻塞进入阻塞与门禁。对应修复提交：`8468f29`。

Claude CLI 的 `auth status` 返回未登录，Claude 桌面端也停在登录页，因此本轮没有伪造 Claude 审查结论；两轮外审由独立只读 Codex 进程完成，模型和推理级别分别为 `gpt-5.6-sol/high` 与 `gpt-5.5/high`。

### 自动验证

- `npm run -s validate`：通过；包含 277 项契约检查、88 条写路由鉴权检查、61 台状态机活性检查、767 项控制台行为断言、崩溃一致性、双进程并发写、空转收敛、仓库写入、组织级 Agent 调度、组织节点动态作用域、工作区流程和任务工作台 API。
- 变异门：1496 条登记的锚点全部唯一；47 条纵深防线明确登记由其它真实门覆盖，其余 1449 条均已证明对应测试能检出破坏。四条必须依赖真实 PostgreSQL/Docker 的变异在释放 `4317` 端口后逐条通过。
- `npm run -s docker:doctor`：通过；覆盖 Compose 配置与镜像、非 root 运行、PostgreSQL CAS、分片缺失／篡改、数据库掉线 503 与自动恢复、集中式 MCP、Agent 安装产物和容器产出 Schema。
- 真实浏览器：两套隔离 Playwright 走查通过，覆盖系统／组织／项目边界、组织与项目 Agent、注册与节点、任务组对象、执行对象、桌面 `1440px` 和窄屏布局。人工浏览器补验确认说明入口、任务组范围清除以及看板跳转后退。

## 验收标准

- 新登录用户不需要知道父页面和栏目关系，即可从菜单直接找到创建项目、成员授权、任务组权限、Agent 档案、注册 Agent、新建任务、启动、监控、审核、复核、指令和仓库配置。
- 桌面侧栏没有“选中父菜单后才出现”的临时栏目；内容区没有承担主要导航职责的 Tab。
- 移动端只有一个功能选择器，不存在横向功能菜单叠加栏目选择器。
- 页面 H1 与实际功能一致，例如打开节点列表时是“运行节点”，而不是先写“项目 Agent”再写“项目运行节点”。
- 深链接、刷新、前进／后退和跨账号重新鉴权保持正确；无权菜单和写操作不出现。
- 不删除已有信息与能力，所有对象详情、实时事件、控制 ACK、Skill／规则、Git 证据和审核记录继续可达。
