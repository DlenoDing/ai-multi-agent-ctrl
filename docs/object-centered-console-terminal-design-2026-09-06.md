# 对象中心管理控制台终态改造

日期：2026-09-06  
状态：已完成

## 目标

把控制台从“页面 + 页面内 Tab”改成可定位、可返回、可持续理解上下文的对象中心管理系统。系统、组织和项目是独立管理空间；项目、任务组、任务、Agent 档案、运行节点、会话与派发是可直接进入的对象。人只负责后台治理、入口控制、必要审核与独立指令，任务拆解、模型和 Skill 选择、调度、执行、过程回送与结果收口继续由 AI 总控和 Agent 自动完成。

## 现状证据

1. 管理空间切换位于顶栏，以两个小型 `section-tab` 呈现；切换后左侧菜单整组替换，但空间本身没有成为侧栏第一层，系统管理与项目管理的边界仍像页面 Tab。
2. 当前项目选择器在顶栏，功能菜单在侧栏，任务组范围在内容区。项目、任务组和任务三个上下文分散在三个区域，进入监控、审核或指令后需要重新确认自己正在操作哪个对象。
3. 所有页面 URL 都是 `/`。页面、栏目、项目、任务组和任务只保存在 `sessionStorage`，不能使用浏览器前进/后退，也不能把某个任务或组织详情的地址交给另一位有权限的管理者复核。
4. 桌面侧栏在当前页面下面嵌套栏目；移动端隐藏该栏目，改在内容顶部再渲染一条横向栏目。项目菜单本身在移动端也是横向滚动，因此实际形成两条需要左右滑动的导航带。
5. 任务组详情已有任务、监控、审核、指令按钮，但离开详情后只剩“任务组范围”下拉；当前任务组进度、任务数、在执行数、待审核数和阻塞数不会持续显示。
6. 任务详情有执行阶段和证据，但项目概览到“创建任务组 → 创建任务 → 启动 → 监控 → 审核/复核”的主线仍散落在不同页面，缺少基于当前状态计算的唯一下一步动作。
7. 组织级与项目级 Agent 已真实参与调度，但两处菜单都叫“AI 智能体”，需要依靠二级栏目才能区分档案、运行节点和注册；当前选中的 Agent/节点也不是可保持的对象上下文。
8. 主渲染存在重复 `app.innerHTML = html`，触摸悬浮处理有重复条件，提交成功词表有重复键。前者会把每次必要渲染的 DOM 解析与写入执行两遍。

## 终态信息架构

### 管理空间

- **系统管理**：系统运行、组织列表、初始组织管理员、组织启停与配额、系统模型和技能源。不得承载组织子账户、项目执行或 Agent 日常操作。
- **组织管理**：组织概况、子账户、项目/任务组权限、组织级 Agent 档案和共享运行节点、项目目录。
- **项目管理**：项目概览、成员、项目级 Agent、任务组、任务、监控、审核、指令、仓库和规则。

管理空间选择器固定在侧栏顶部，采用完整名称、当前状态和对象数量，不再作为顶栏小 Tab。项目选择器与项目模块导航放在同一侧栏上下文中。

### 对象上下文

- 侧栏持续显示当前项目；选定任务组后显示任务组名称、进度、状态、任务/运行/待审/阻塞计数。
- 进入任务后继续显示当前任务、负责角色、状态和进度，并提供返回任务组或任务列表的直接动作。
- 内容区保留可点击层级路径，但不重复侧栏的完整信息。
- 桌面使用侧栏对象导航；窄屏使用一个“当前位置/切换栏目”控制，不叠加两条横向 Tab。

### 可寻址工作区

地址只保存对象标识与界面位置，不保存令牌、规则正文、任务正文或结果内容。目标格式：

```text
#/system/organizations/<orgId>
#/organization/members/<accountId>
#/project/<projectId>/task-groups/<taskGroupId>
#/project/<projectId>/tasks/<taskGroupId>/<workItemId>
#/project/<projectId>/monitor/<taskGroupId>
#/project/<projectId>/monitor/<taskGroupId>/session/<sessionId>
#/project/<projectId>/monitor/<taskGroupId>/dispatch/<dispatchId>
#/project/<projectId>/agents/runtime/<nodeId>
#/organization/agents/runtime/<nodeId>
```

登录后按当前账号重新鉴权和读取对象；无权、已删除或跨组织对象必须给出明确提示并回到最近可见上级。浏览器前进/后退必须恢复页面、栏目和对象，不依赖旧账号的本地缓存。

### 操作主线

项目概览只呈现一条按状态计算的主操作：配置仓库、接入 Agent、创建任务组、创建任务、启动任务组、查看执行、处理审核、处理阻塞或查看结果。其它操作保留在对象对应模块，不再同时铺开成说明卡片。

任务组对象提供固定控制头：进度、任务总数、在执行数、待审核数、阻塞数、当前语言和运行状态；任务、监控、审核、指令、Agent 是对象级动作，不要求再次选择任务组。

## 实施顺序

1. [已实现] 新增侧栏管理空间与对象上下文模块，移动管理空间和项目选择，消除顶栏 Tab 语义。
2. [已实现] 新增安全 URL 路由模块，接入菜单、栏目、项目、任务组、任务、组织与成员/Agent 详情以及浏览器前进/后退。
3. [已实现] 增加任务组持续状态摘要和项目主操作，贯通创建、启动、监控、审核、复核、指令与结果查看。
4. [已实现] 收敛 Agent 命名和对象入口，补齐组织/项目 Agent 档案查看、编辑、启停和独立地址。
5. [已实现] 移除重复导航和本轮重复统计扫描，验证实时刷新、草稿保护和路由不会互相覆盖。
6. [已实现] 完成系统管理员、组织管理员、项目管理员的桌面/390px 隔离浏览器操作，并复验既有只读观察者权限门。
7. [已实现] 完成 Claude 安全、状态一致性和中文 UX 多轮复审，修正有效发现后逐步提交推送。

## 验收条件

- 登录后无需猜测即可区分系统、组织和项目管理空间，系统空间不出现组织子账户或项目执行操作。
- 任一项目执行页面都能看到当前项目；选定任务组或任务后，上下文持续存在且可直接返回上级。
- 可复制地址打开同一对象；另一账号打开时重新鉴权，不能读到缓存中的对象内容。
- 浏览器前进/后退恢复真实对象与栏目，未保存表单仍执行离开确认。
- 从空项目到任务执行、监控、审核/复核和结果查看，每一步只有清楚的当前状态与下一主操作。
- 任务详情完整保留 Agent、运行节点、角色、模型、推理、Skill、规则、事件、Git 证据和结果，不以简化导航为由删除信息。
- 组织级和项目级 Agent 均可建立、注册、查看和控制；任务组自动调度仍可同时选择两级 Agent。
- 390px 窄屏不存在双横向导航、页面级横向溢出、文字重叠或不可达操作。
- 全量验证、关键变异、真实浏览器与外部互审无未处置高风险问题。

## 实现结果

- 管理空间选择固定在侧栏第一层。系统管理员只进入系统管理，组织管理员可在组织管理与有权项目间切换，普通成员只进入有权项目；顶栏不再使用空间 Tab。
- 当前项目选择、项目状态和进度与项目模块同处侧栏。选中任务组后持续显示任务、运行、待审、受阻统计；进入任务后继续显示当前任务名称、状态和进度。
- 移动端二级栏目由横向 Tab 改为单一“当前栏目”选择器；当前任务卡保持可见，不通过隐藏对象换取短页面。
- URL 路由支持系统组织、组织成员、项目成员、组织/项目 Agent 档案、任务组、任务、监控、审核和指令。浏览器前进/后退恢复同一对象；路由仅包含白名单对象 ID 和栏目，不含令牌或正文。
- 浏览器历史恢复采用单飞和最新路由排队。未保存表单取消离开时会丢弃排队路由并恢复当前地址，不会并发弹出多个确认框。
- 不可见或不存在的深链接对象会在服务端过滤完成后清除定位并明确提示；摘要窗口外的合法任务组通过单对象接口补回任务组、会话、派发、产出与关闭门上下文。
- 项目概览新增唯一“当前下一步”，按仓库、节点、任务组、任务、暂停、审核、阻塞、运行、结果顺序计算。只读账号不会被带入隐藏创建栏目。
- 组织空间菜单明确为“共享 Agent”，项目空间明确为“项目 Agent”；栏目分别标出共享/项目运行节点、注册和档案。
- Agent 档案新增 `POST /api/agents/:id/profile`。可更新名称、角色、模型偏好、信任分和 Skill；ID、组织/项目作用域、状态与容量不可改写，启停走独立接口。项目空间只能查看组织共享档案，必须回组织空间修改。
- Agent 档案更新为真人专属治理动作；运行中的总控仍只产生任务级模型与 Skill 决策，不持久改写档案。
- 项目/任务组运行统计改为按状态快照一次建索引，侧栏、项目主操作和任务组详情复用，避免按任务组重复扫描全部派发与审核记录。
- 会话、派发和运行节点现已补齐独立对象详情、服务端单对象投影与安全深链接；任务、监控表格和节点负载都进入同一条执行对象链。项目／任务组监控使用不同对象头，不再只靠范围下拉区分。

## 验证与互审

- 控制台行为门最终覆盖 823 条断言；契约门 277 条；写路由鉴权门覆盖 88 条改状态路由；真人专属动作共 37 条。
- 变异门共 1,572 条登记守卫，全部完成实际破坏验证；本轮新增的对象路由、持续上下文、移动栏目、项目主操作、Agent 管理、执行对象／节点详情、权限负例、统计缓存和窗口外对象补回均已证明判别力。
- 完整 `npm run validate` 通过，包含初始化启动、分片、并发写、崩溃恢复、真实 Agent 注册/Skill 同步/领活/过程事件/Git 推送/检查点回送和跨组织隔离。
- 隔离 Playwright 真实创建组织、组织管理员、项目、任务组、任务、组织共享 Agent 和项目 Agent；系统管理员与组织管理员分别登录。390×844 的系统组织详情、共享 Agent 编辑、任务详情及 1440×960 项目 Agent 编辑均无页面级横向溢出或浏览器错误。
- 内置浏览器验证任务深链接刷新、任务到监控跳转、浏览器后退恢复任务，以及系统/项目空间历史切换。
- Claude UX 复审未发现阻断问题；安全复审未发现 P0/P1，提出的浏览器历史重入与窗口外任务组边界均已修正。修正后聚焦复审确认单飞、取消和不可见对象清理逻辑成立。

## 代码边界

本轮新增 `context-navigation.js`、`workspace-route.js`、`project-command-center.js`、`agent-profile-workspace.js`、`operational-stats.js`、`execution-object-workspace.js`、`monitor-workspace.js` 和 `runtime-node-workspace.js`。后续又迁出项目设置、任务组详情和完整监控模块；服务端详情聚合拆入 `execution-object-detail.mjs` 与 `runtime-node-detail.mjs`，核心命令总线和运行问题追踪也已独立。历史 `app.js` 仍承担页面状态、数据加载和事件装配，当前 9,158 行；它是兼容门面，不是“已彻底消除大文件”的声明，后续拆分仍需保持现有全量门和浏览器行为不变。
